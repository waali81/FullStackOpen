{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Timo Waali",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../puhluettelofront && npm run build && cp -r dist ../puhluetteloback",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
