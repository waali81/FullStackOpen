{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Timo Waali",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue; cd ../puhluettelofront; npm run build; Copy-Item -Recurse -Force dist ../puhluetteloback",
    "deploy:full": "npm run build:ui; git add .; git commit -m \"uibuild\"; git push",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
